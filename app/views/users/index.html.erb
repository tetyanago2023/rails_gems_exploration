<h2><%= true_user.email %>, please make your choise to pretend as one of the user listed below:</h2>
<ul>
  <% @users.each do |user| %>
    <li>Sign in as <%= link_to user.email, impersonate_user_path(user), data: { turbo_method: :post } %></li>
  <% end %>
</ul>
